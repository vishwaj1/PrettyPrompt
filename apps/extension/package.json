{
  "name": "extension",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "start": "next start",
    "lint": "next lint",
    "tsup":  "tsup --config tsup.config.ts",
    "build": "npm run tsup && next build && mkdir -p dist && mv .next/static dist/ui || true && cp public/manifest.json dist && cp -r public/icons dist/icons"

    ,"zip":   "cd dist && zip -r ../PrettyPromptExt.zip *"
  },
  "dependencies": {
    "next": "15.3.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/chrome": "^0.0.317",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "15.3.1",
    "tailwindcss": "^4",
    "tsup": "^7",
    "typescript": "^5"
  }
}
